pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_NON_GAINING_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "WebsiteToApk"
include ':app'
